import AvMicroserviceApi from '../ms';

export default class Av{{pascalCase resourceName}} extends AvMicroserviceApi {
  constructor(config) {
    super({
      path: '',
      name: '',
      ...config
    });
  }
}

