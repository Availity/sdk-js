const {{pascalCase packageName}} = require('../src/{{kebabCase packageName}}')

describe('{{kebabCase packageName}}', () => {
  test('should exist', () => {
    expect({{pascalCase packageName}}).toBeDefined();
  });
});
